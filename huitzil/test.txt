strict

a = 15

b = 10
#> comentario
multilinea <# 
if ( a = b )
(
    # comentario linea
    output ("number are equals")
) elif ( a > b) (
    output ("a is greater")
) else (
    output ("b is greater")
)

b = true
while (b)
(
    output("hello_world!".trim)
    answer = input("Again? [n/y]")
    if (answer == "n") (
        b = false
    ) else (
        b = true
    )
)

#non strict mode
number = 12
number = 12.5
number = 12.5e20
number = 12 + 4i
text = 'a'
text = 'hello'

var number #undefined

#strict mode for more  professional, complex software
var number_int_null = int() #null
var number_int = int(12)
var number_complex = float(12.5)
var number_snot = snot(12.5e20)
var number_complex = complex(12 + 4i)
var text_char = char('a')
var text_string = 'hello'

text_to_upper = text.upper()

#set can be a tuple, dictionary or simply a set
set = {"a","b",15,12}
#explicit declaration for a tuple
tuple = tuple({"a","b",15,12})

#same case with arrays
#an array can be a list, queue or stack
array = ["a","b","c"]
stack = stack(["a","b","c"])

#class, default private
public class myclass
(
    private attribute counter1 = int()
    public attribute counter2 = int()
    internal attribute counter3 = int()
    protected attribute counter4 = int()
    #construct
    construct
    (

    )

    #simple method
    method()
    (
        #default private
        attribute name = string("")
    )

    #destruct
    destruct
    (

    )
)
